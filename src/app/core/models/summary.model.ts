export interface GroupedData {
    totalScore: number;
    percentage: number;
    items: any[];
}
  
  export interface GroupedResult {
    section: string;
    groupName: string;
    totalScore: number;
    percentage: number;
    items: any[];
}
